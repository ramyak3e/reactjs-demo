pipeline {
	agent any

	stages {
		stage('Build and Push Docker Image') {
			steps {
				script {
					if (env.BRANCH_NAME == 'origin/master') {
                        echo 'Hello'
                    }

                    }
            }
        }
    }
}
