pipeline {
	agent any

	stages {
		stage('Build and Push Docker Image') {
			steps {
				script {
					if (env.BRANCH_NAME == 'dev') {
                        echo 'Hello from ${env.BRANCH_NAME} branch!'
                    }
                    }
            }
        }
    }
}
